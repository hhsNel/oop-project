name: Checklist Enforcement

on:
  pull_request:
    types: [opened, edited, synchronize, reopened]

jobs:
  check-tasks:
    runs-on: ubuntu-latest
    steps:
      - name: Check if all checkboxes are completed
        uses: tzkhan/pr-checklist-action@v1
        with:
          checkout_all: true

